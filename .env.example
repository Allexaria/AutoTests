# GitLab token used only for Docker build-time cloning.
# Never commit real token values to git.
GITLAB_API_TOKEN=replace_with_your_token
GITLAB_GROUP_PATH=barbariki245
GITLAB_API_URL=https://gitlab.com/api/v4
